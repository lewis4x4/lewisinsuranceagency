# Lead Storage

This directory stores leads when `LEADS_DESTINATION=local` (default for development).

**DO NOT commit this file to version control with real lead data.**

The `leads.json` file will be created automatically when the first lead is submitted.
